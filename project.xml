<project>
  <files>
    <file name="Input.jsx" path="Input.jsx">
      <summary><![CDATA[import {cn} from "@/shared/utils/cn";
export default function Input({label,error,className,disabled,...props}){
    return (
        <div className="flex flex-col gap-1 w-full">
            {la...]]></summary>
      <content><![CDATA[import {cn} from "@/shared/utils/cn";

export default function Input({label,error,className,disabled,...props}){
    return (
        <div className="flex flex-col gap-1 w-full">
            {label && (
                <label className="text-sm font-medium text-slate-700">
                    {label}
                </label>
            )}
            <input className={cn(
                "h-10 w-full rounded-xl border border-slate-300 px-3 text-sm",
                "focus:outline-none focus:ring-2 focus:ring-slate-400",
                "disabled:opacity-50 disabled:cursor-not-allowed",
                error && "border-red-500 focus:ring-red-400",
                className
            )}
            disabled={disabled}
            {...props}
            />
            {error && (
                <span className="text-xs text-red-500">
                    {error}
                </span>
            )}
        </div>
    );
}]]></content>
    </file>
  </files>
</project>
